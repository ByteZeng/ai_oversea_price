# 指标字典样例（精简版）

本样例用于演示“指标字典”应如何存放常用指标与其计算口径，方便后续 LLM 在分析用户问题时先检索指标，再生成 SQL。

## 1) 字段（精简必需）

字段说明见：`profit_analyst_mvp/docs/metric_dictionary_fields.md`

## 2) 样例表（可视化）

我把“样例表”做成了一个可视化文档，便于浏览与后续扩展（避免在 Markdown 里维护大表格）。

- Canvas 文件：`C:\Users\Administrator\.cursor\projects\f-cursor-workspace-ai-oversea-price\canvases\metric-dictionary-sample.canvas.tsx`

在 Cursor 里打开该文件后，可在右侧以 Canvas 方式预览。

